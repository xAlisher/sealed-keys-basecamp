{
  description = "Sealed — receive-key collection Basecamp module (0.1.0)";
  # Real build uses the Logos module builder flake (see logos-wallet-basecamp/flake.nix).
  # Scaffold placeholder: pin the module-builder input here when wiring the nix build.
}
