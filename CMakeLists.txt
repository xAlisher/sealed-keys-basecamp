cmake_minimum_required(VERSION 3.14)
project(SealedKeysModulePlugin LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

# Logos Module CMake helper (provided by logos-module-builder). The universal
# interface is derived from src/sealed_keys_impl.h (metadata.json codegen); the
# helper generates the plugin glue and compiles the sources below.
if(DEFINED ENV{LOGOS_MODULE_BUILDER_ROOT})
    include($ENV{LOGOS_MODULE_BUILDER_ROOT}/cmake/LogosModule.cmake)
elseif(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/cmake/LogosModule.cmake")
    include(cmake/LogosModule.cmake)
else()
    message(FATAL_ERROR "LogosModule.cmake not found. Set LOGOS_MODULE_BUILDER_ROOT.")
endif()

logos_module(
    NAME sealed_keys
    SOURCES
        src/sealed_keys_impl.h
        src/sealed_keys_impl.cpp
    INCLUDE_DIRS
        src
)
